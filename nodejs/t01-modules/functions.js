module.exports.counter = function (input) {
	return 'There are ' + input.length + ' elements in this array';
};

module.exports.adder = function (a, b) {
	return `The sum of the 2 numbers is ${a+b}`;
};

module.exports.pi = 3.1415;
